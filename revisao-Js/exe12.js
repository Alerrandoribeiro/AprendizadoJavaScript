/**Escreva uma função que imprima no console o ano atual baseado na "Data Atual” do computador. */
function exercicioDoze(){
    let data =  new Date().getFullYear();
    alert('O ano atual é ' + data)
}
